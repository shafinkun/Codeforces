#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,a;
    string s;
    while(t--)
    {
        cin>>n;
        cin>>s;
        for(int i=0;i<n;i++)
        {
            if(s[i]=='.') continue;
            else if(s[i]==s[i]=='H') a=0;
            else if(s[i]=='T') a=1;
        };
        if(a)
    }
    return 0;
}